server:
  # 服务器端口号
  port: 81
  servlet:
    context-path: /
spring:
  application:
    name: OnlineDocs
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://aliyun.jisy.me:3306/cmd?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: doc
    password: AliyunDocApplication
mybatis:
  mapper-locations: classpath*:/mapper/*.xml
